server:
  port: 2002
spring:
  datasource:
      url: jdbc:postgresql://localhost:5432/CRM
      username: postgres
      password: root
  jpa:
    show-sql: true
    properties:
      hibernate: 
        dialect: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
